# Анализ данных онлайн-игры — SQL + аналитика

## 📊 О проекте

Проект выполнен для команды разработки онлайн-игры. Основная цель — изучить влияние характеристик игроков (раса персонажа) на покупку внутриигровой валюты, а также проанализировать активность игроков при совершении внутриигровых покупок эпических предметов.

**Ключевые вопросы исследования:**
- Какова доля платящих игроков и зависит ли она от расы персонажа?
- Какие статистические характеристики имеют внутриигровые покупки (стоимость, количество, аномалии)?
- Есть ли предметы, которые покупают чаще других?
- Существует ли зависимость активности покупок от расы персонажа?

В ходе проекта были написаны SQL-запросы (в DBeaver), проведён исследовательский анализ данных, решены ad hoc задачи и сформулированы выводы с рекомендациями для маркетинговой команды.

## 🛠 Инструменты

- **SQL** (PostgreSQL) — написание запросов, агрегация, статистические расчёты
- **DBeaver** — подключение к базе данных, выполнение запросов
- **Git / GitHub** — управление версиями и публикация портфолио

## 📁 Структура репозитория
darkwood-secrets-game-analytics/  
├── README.md  
├── /sql  
│ └── darkwood_queries.sql # все SQL-запросы проекта  
├── /docs  
│ └── analytical_report.md # полная аналитическая записка с выводами  

## 🗃 Ключевые SQL-запросы

Полный список всех запросов доступен в файле [`sql/darkwood_queries.sql`](./sql/darkwood_queries.sql). Ниже представлены наиболее важные запросы, иллюстрирующие ход анализа.

### 1. Доля платящих игроков в разрезе расы персонажа

```
SELECT
    r.race,
    SUM(u.payer) AS total_payers,
    COUNT(u.payer) AS total_players,
    AVG(u.payer)::NUMERIC(5,4) AS share_payers
FROM fantasy.users AS u
JOIN fantasy.race AS r USING(race_id)
GROUP BY r.race
ORDER BY total_players DESC;
```
**Результаты**: доля платящих игроков стабильна (~18%) и практически не зависит от расы.

### 2. Статистика стоимости покупок (без учёта нулевых)
```
SELECT
    COUNT(amount) AS total_amount,
    SUM(amount) AS sum_amount,
    MIN(amount) AS min_amount,
    MAX(amount) AS max_amount,
    AVG(amount)::NUMERIC(6,2) AS avg_amount,
    PERCENTILE_CONT(0.5) WITHIN GROUP (ORDER BY amount)::NUMERIC(6,2) AS median,
    STDDEV(amount)::NUMERIC(6,2) AS stand_dev
FROM fantasy.events
WHERE amount > 0;
```
**Результаты**: средняя стоимость 525,69, медиана 74,86 — сильный разброс указывает на наличие выбросов.

### 4. Популярность эпических предметов (доля продаж и доля покупателей)
```
WITH item_info AS (
    SELECT
        i.item_code,
        i.game_items,
        COUNT(e.amount) AS item_sell,
        COUNT(DISTINCT e.id) AS unique_id
    FROM fantasy.events AS e
    JOIN fantasy.items AS i ON e.item_code = i.item_code
    WHERE amount > 0
    GROUP BY i.item_code
),
events_info AS (
    SELECT
        COUNT(amount) AS total_buy,
        COUNT(DISTINCT id) AS total_unique_id
    FROM fantasy.events
    WHERE amount > 0
)
SELECT
    game_items,
    (item_sell::real / (SELECT total_buy FROM events_info))::NUMERIC(5,4) AS per_buy,
    (unique_id::real / (SELECT total_unique_id FROM events_info))::NUMERIC(5,4) AS per_players
FROM item_info
ORDER BY per_buy DESC;
```
**Результаты**: три предмета (Book of Legends, Bag of holding, Necklace of Wisdom) составляют 98% всех продаж, 40 предметов ни разу не куплены.

### 5. Активность игроков в разрезе рас (среднее количество покупок, средняя сумма)
```
WITH users_race AS (
    SELECT race_id, COUNT(u.id) AS total_users
    FROM fantasy.users AS u
    GROUP BY race_id
),
payer_race AS (
    SELECT race_id,
           COUNT(id) AS buyer_players,
           AVG(payer)::NUMERIC(5,4) AS per_payer_players
    FROM fantasy.users
    WHERE id IN (SELECT DISTINCT id FROM fantasy.events WHERE amount > 0)
    GROUP BY race_id
),
users_statistics AS (
    SELECT race_id,
           COUNT(transaction_id) AS total_buyers,
           SUM(amount) AS total_amount
    FROM fantasy.events AS e
    JOIN fantasy.users AS u ON e.id = u.id
    WHERE amount > 0
    GROUP BY race_id
)
SELECT
    r.race,
    ur.total_users,
    pr.buyer_players,
    (pr.buyer_players / total_users::real)::NUMERIC(5,4) AS per_buyer_players,
    pr.per_payer_players,
    (us.total_buyers / pr.buyer_players::real)::NUMERIC(5,2) AS avg_buy_player,
    (us.total_amount / us.total_buyers::real)::NUMERIC(5,2) AS avg_amount_player,
    (us.total_amount / pr.buyer_players::real)::NUMERIC(7,2) AS avg_amount_sum
FROM users_race AS ur
JOIN fantasy.race AS r ON ur.race_id = r.race_id
JOIN payer_race AS pr ON ur.race_id = pr.race_id
JOIN users_statistics AS us ON ur.race_id = us.race_id
ORDER BY total_users DESC;
```
**Результат**: люди и ангелы покупают значительно больше предметов (121 и 107 против 81 у остальных рас), что опровергает гипотезу о равной сложности игры.

## 📈 Результаты и выводы
Полная аналитическая записка находится в файле docs/analytical_report.md. Ниже представлены ключевые тезисы.

### 1. Доля платящих игроков
В среднем по игре доля платящих игроков составляет ~18%.  
Показатель стабилен и практически не зависит от расы персонажа (расхождение в пределах 2%).

### 2. Статистика внутриигровых покупок
- Всего совершено 1 307 678 покупок на сумму 686 615 040 единиц валюты.  
- Минимальная стоимость – 0, максимальная – 486 615,1.  
- Средняя стоимость – 525,69, медиана – 74,86, стандартное отклонение – 2 517,35.  
Значительный разброс указывает на наличие сильных выбросов.

### 3. Аномальные покупки
Обнаружено 907 покупок с нулевой стоимостью (доля – 0,07% от всех покупок). Они не приносят дохода и могут быть артефактами или тестовыми операциями.

### 4. Популярность эпических предметов
Из 184 предметов три составляют 98% всех продаж:  
- Book of Legends – 77% покупок, 88% покупателей.  
- Bag of holding – 21% покупок, 87% покупателей.  
- Necklace of Wisdom – 11% покупок, 12% покупателей.  

40 предметов ни разу не покупались – возможно, они не сбалансированы или неинтересны игрокам.

### 5. Зависимость активности от расы
Доля покупающих игроков (60–63%) и доля платящих (16–20%) по расам близки.  
Однако среднее количество покупок на одного покупателя резко различается:  
- Люди: 121 (в 1,5 раза выше среднего).  
- Ангелы: 107 (на 32% выше среднего).  
- Остальные расы: около 81.  

Это говорит о том, что для прохождения за людей и ангелов требуется значительно больше эпических предметов, что опровергает гипотезу о равной сложности игры для всех рас.

## 💡Рекомендации
1. Сбалансировать сложность игры. Для людей и ангелов количество необходимых покупок существенно выше. Стоит проанализировать игровой баланс и, возможно, скорректировать характеристики этих рас или доступность эпических предметов.  
2. Оптимизировать ассортимент эпических предметов. 40 предметов никогда не покупались – их можно убрать или изменить их свойства, чтобы повысить привлекательность.  
3. Исследовать причины нулевых покупок. Возможно, это сбои или тестовые операции; их следует исключить из отчётности.  
4. Учитывать расу в маркетинговых кампаниях. Поскольку люди и ангелы покупают больше, имеет смысл таргетировать рекламу на игроков этих рас, предлагая им специальные предложения.
